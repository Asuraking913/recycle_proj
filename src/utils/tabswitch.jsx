const handleUpload = (onClicked) => {
    onClicked("upload")
}

export const handleOrder = (onClicked) => {
    onClicked("orders")
}

export const handlePayment = (onClicked) => {
    onClicked("payments")
}

export const handleReceipts = (onClicked) => {
    onClicked("receipts")
}

export default handleUpload